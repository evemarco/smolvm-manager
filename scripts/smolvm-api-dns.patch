diff --git a/src/api/handlers/machines.rs b/src/api/handlers/machines.rs
index df1042f..d1c4366 100644
--- a/src/api/handlers/machines.rs
+++ b/src/api/handlers/machines.rs
@@ -851,6 +851,13 @@ pub async fn create_machine(
     let restored_allowed_hosts = checkpoint_network
         .and_then(|network| network.dns_filter_hosts.clone())
         .or_else(|| req.allowed_hosts.clone());
+    // The checkpoint's captured DNS override wins (same precedence as the
+    // restored egress policy); for a fresh machine the request value is used,
+    // and without either the backend default applies.
+    let restored_dns = checkpoint_network
+        .and_then(|network| network.dns.as_deref())
+        .and_then(|dns| dns.parse().ok())
+        .or(req.dns);
     if let Some(network) = checkpoint_network {
         normalized_cidrs = network.allowed_cidrs.clone();
     }
@@ -897,9 +904,12 @@ pub async fn create_machine(
     }
     .validate()
     .map_err(|e| ApiError::BadRequest(e.to_string()))?;
+    // CLI parity: `--dns` implies --net (see resolve_egress_flags and
+    // smolfile.rs), and a nameserver without any network backend is a silent
+    // no-op — force the network on when the caller set one.
     let network = checkpoint_network
         .map(|network| network.enabled)
-        .unwrap_or(req.network || manifest_net);
+        .unwrap_or(req.network || manifest_net || restored_dns.is_some());
 
     // Reserve the name atomically (prevents concurrent creation)
     let guard = ReservationGuard::new(&state, name.clone())?;
@@ -1097,6 +1107,7 @@ pub async fn create_machine(
         allowed_cidrs: normalized_cidrs,
         allowed_hosts: restored_allowed_hosts,
         network_backend: restored_network_backend,
+        dns: restored_dns,
     };
 
     // Validate request-body secret refs before persisting. Untrusted
@@ -3893,6 +3904,7 @@ mod tests {
             allowed_cidrs: None,
             allowed_hosts: None,
             network_backend: None,
+            dns: None,
             restart: None,
             image: None,
             from: None,
diff --git a/src/api/state.rs b/src/api/state.rs
index 7589c76..abf6d0e 100644
--- a/src/api/state.rs
+++ b/src/api/state.rs
@@ -505,6 +505,7 @@ impl ApiState {
                 allowed_cidrs: record.allowed_cidrs.clone(),
                 allowed_hosts: record.dns_filter_hosts.clone(),
                 network_backend: record.network_backend,
+                dns: record.dns,
             };
 
             // Create AgentManager and try to reconnect
@@ -1020,6 +1021,7 @@ impl ApiState {
         record.allowed_cidrs = reg.resources.allowed_cidrs.clone();
         record.dns_filter_hosts = reg.resources.allowed_hosts.clone();
         record.network_backend = reg.resources.network_backend;
+        record.dns = reg.resources.dns;
         // GPU flags (previously dropped here, so API-created machines
         // silently lost CUDA/GPU on restart).
         record.gpu = reg.resources.gpu;
@@ -1638,9 +1640,9 @@ pub fn resource_spec_to_vm_resources(spec: &ResourceSpec, network: bool) -> VmRe
         overlay_gib: spec.overlay_gb,
         block_io: spec.block_io.unwrap_or_default(),
         allowed_cidrs: spec.allowed_cidrs.clone(),
-        // Custom DNS is a local-CLI feature for now; the cloud ResourceSpec
-        // does not expose it, so API-launched VMs inherit the backend default.
-        dns: None,
+        // ResourceSpec carries the per-machine custom DNS override (API parity
+        // with the CLI `--dns`); None keeps the backend default.
+        dns: spec.dns,
         network_name: None,
     }
 }
@@ -1661,6 +1663,7 @@ pub fn vm_resources_to_spec(res: VmResources) -> ResourceSpec {
         // back from the source record (see the MachineEntry reload path).
         allowed_hosts: None,
         network_backend: res.network_backend,
+        dns: res.dns,
     }
 }
 
@@ -1852,6 +1855,7 @@ mod tests {
             allowed_cidrs: None,
             allowed_hosts: None,
             network_backend: None,
+            dns: None,
         };
         let res = resource_spec_to_vm_resources(&spec, false);
         assert_eq!(res.cpus, DEFAULT_MICROVM_CPU_COUNT);
@@ -1918,6 +1922,7 @@ mod tests {
                     allowed_cidrs: None,
                     allowed_hosts: None,
                     network_backend: None,
+                    dns: None,
                 },
                 restart: RestartConfig::default(),
                 network: false,
@@ -1986,6 +1991,7 @@ mod tests {
                     allowed_cidrs: None,
                     allowed_hosts: None,
                     network_backend: None,
+                    dns: None,
                 },
                 restart: RestartConfig::default(),
                 network: false,
diff --git a/src/api/types.rs b/src/api/types.rs
index 07cd4a6..08413c6 100644
--- a/src/api/types.rs
+++ b/src/api/types.rs
@@ -132,6 +132,12 @@ pub struct ResourceSpec {
     /// lighter outbound-only backend (rejected alongside published `ports`).
     #[serde(default)]
     pub network_backend: Option<crate::network::NetworkBackend>,
+    /// Custom guest DNS resolver (single IPv4). Overrides the backend default
+    /// for this machine: written into the guest resolv.conf under TSI, or used
+    /// as the gateway upstream under virtio-net.
+    #[serde(default)]
+    #[schema(value_type = String)]
+    pub dns: Option<std::net::Ipv4Addr>,
 }
 
 // ============================================================================
@@ -628,6 +634,13 @@ pub struct CreateMachineRequest {
     /// path), so that combination is rejected.
     #[serde(default)]
     pub network_backend: Option<crate::network::NetworkBackend>,
+    /// Custom guest DNS resolver (single IPv4), same semantics as the CLI
+    /// `machine create --dns`: written into the guest resolv.conf under TSI,
+    /// or used as the gateway upstream under virtio-net. Omit for the backend
+    /// default.
+    #[serde(default)]
+    #[schema(value_type = String)]
+    pub dns: Option<std::net::Ipv4Addr>,
     /// Restart policy configuration.
     #[serde(default)]
     pub restart: Option<RestartSpec>,
